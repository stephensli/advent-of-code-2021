package main

import (
	"github.com/stephensli/aoc/helpers/aoc"
)

func main() {
	path, complete := aoc.Setup({{ .Year }}, {{ .Day }}, true)
	defer complete()

	aoc.PrintAnswer(1, path)
	aoc.PrintAnswer(2, path)
}